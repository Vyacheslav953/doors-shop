const base =
  'inline-flex items-center justify-center rounded-full px-6 py-3 text-sm font-semibold transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-bronze'

export const btnPrimary = `${base} bg-bronze text-white hover:bg-bronze-dark`
export const btnGhost = `${base} border border-bronze text-bronze hover:bg-bronze hover:text-white`
